import React from "react";
import Input from "./Input";

const Form = ({ data, errors }) => {
  // TODO:
  // 封装input，select等表单元素，
  // 但是由于functional component不可继承
  // 暂时未找到解决办法
  function renderInput() {
    return null;
  }
  return null;
};

export default Form;
